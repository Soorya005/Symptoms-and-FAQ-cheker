import React from 'react';
import ReactDOM from 'react-dom';
import Ap from './App'; // Import your main component

ReactDOM.render(
  <React.StrictMode>
    <Ap /> {/* Use the main component */}
  </React.StrictMode>,
  document.getElementById('root')
);
